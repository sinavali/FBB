export enum CandleDirections {
  Up = "up",
  Down = "down",
  Idle = "idle",
}

export enum ProjectName {
  FBB_Bot = "fbb_bot",
  FBB_Dashboard = "fbb_dashboard",
}

export enum MetaTraderSocketType {
  CMD = "cmd",
  DATA = "data",
}
